## What

<!-- One or two sentences. -->

## Why

<!-- The problem this solves. -->

## Checks

- [ ] `uv run --extra dev pytest` passes
- [ ] I rebuilt the frontend if I changed `frontend/` (`npm --prefix frontend run build`)
- [ ] I did not commit SQLite files, thumbnails, library dumps, `.env`, or device backups
- [ ] The local server still binds to `127.0.0.1` only
- [ ] README or docs are updated if a flag, default, or safety behavior changed
